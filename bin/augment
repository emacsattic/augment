#!/usr/bin/env ruby
$LOAD_PATH << File.dirname(__FILE__) + '/../lib'

require 'augment'

if ARGV.size != 2
  puts "Usage: augment BACKEND FILE"
  exit 1
end

if ARGV.first == 'background'
  Augment.background(*ARGV)
  exit 0
end

Augment.new(*ARGV)
