#!/usr/bin/env ruby
$LOAD_PATH << File.dirname(__FILE__) + '/../lib'

require 'augment'

if ARGV.first == 'background'
  Augment.background
  exit 0
elsif ARGV.size != 2
  puts "Usage: augment BACKEND FILE"
  exit 1
end

Augment.new(*ARGV)
